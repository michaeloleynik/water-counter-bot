# Инструкция по установке и запуску

## Требования

- Node.js (версия 18 или выше)
- PostgreSQL (версия 12 или выше)
- npm или yarn

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Создание бота в Telegram

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например: "Учет счетчиков воды")
4. Введите username для бота (должен заканчиваться на "bot", например: "water_counter_bot")
5. BotFather отправит вам токен бота (сохраните его!)

## Шаг 3: Настройка PostgreSQL

### Windows:

1. Скачайте и установите PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/)
2. Во время установки запомните пароль для пользователя `postgres`
3. Откройте pgAdmin или командную строку PostgreSQL
4. Создайте базу данных:

```sql
CREATE DATABASE water_counter_bot;
```

### Альтернатива (Docker):

```bash
docker run --name postgres-water-counter -e POSTGRES_PASSWORD=your_password -e POSTGRES_DB=water_counter_bot -p 5432:5432 -d postgres:15
```

## Шаг 4: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:

```bash
copy .env.example .env
```

2. Откройте файл `.env` и заполните все необходимые данные:

```env
# Токен бота из BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Настройки базы данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=water_counter_bot
DB_USER=postgres
DB_PASSWORD=ваш_пароль_от_postgres

# Порт сервера
PORT=3000
NODE_ENV=development

# Директория для загрузки файлов
UPLOAD_DIR=./uploads

# Ваш Telegram ID (для первого администратора)
# Узнать свой ID можно у бота @userinfobot
DEFAULT_ADMIN_TELEGRAM_ID=ваш_telegram_id
```

## Шаг 5: Инициализация базы данных

Выполните миграции для создания таблиц:

```bash
npm run db:migrate
```

Вы должны увидеть сообщения:
```
✅ Миграции успешно выполнены
✅ Создан администратор с Telegram ID: ваш_id
```

## Шаг 6: Запуск бота

### Режим разработки (с hot-reload):

```bash
npm run dev
```

### Продакшн режим:

```bash
# Сборка проекта
npm run build

# Запуск
npm start
```

Если все настроено правильно, вы увидите:
```
✅ Бот успешно запущен и готов к работе!
📝 Нажмите Ctrl+C для остановки
```

## Шаг 7: Первый запуск

1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Если вы указали правильный `DEFAULT_ADMIN_TELEGRAM_ID`, вы будете зарегистрированы как администратор
4. Используйте кнопки меню для навигации

## Проверка работы

### Проверка подключения к БД:

Откройте pgAdmin или выполните SQL запрос:

```sql
SELECT * FROM users;
```

Вы должны увидеть своего пользователя с ролью 'admin'.

### Проверка загрузки файлов:

Убедитесь, что директория `uploads/` была создана в корне проекта.

## Возможные проблемы

### Ошибка подключения к БД

```
Error: connect ECONNREFUSED 127.0.0.1:5432
```

**Решение:** Проверьте, что PostgreSQL запущен и доступен на порту 5432.

### Ошибка "BOT_TOKEN не указан"

**Решение:** Убедитесь, что файл `.env` создан и содержит корректный `BOT_TOKEN`.

### Бот не отвечает

**Решение:** 
1. Проверьте, что бот запущен (должно быть сообщение "✅ Бот успешно запущен")
2. Проверьте токен бота
3. Проверьте логи в консоли на наличие ошибок

### Ошибка "У вас нет доступа"

**Решение:** 
1. Убедитесь, что вы указали правильный `DEFAULT_ADMIN_TELEGRAM_ID` в `.env`
2. Проверьте свой Telegram ID у бота @userinfobot
3. Перезапустите миграции: `npm run db:migrate`

## Дополнительно

### Как узнать свой Telegram ID:

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему любое сообщение
3. Он вернет ваш ID

### Создание резервной копии БД:

```bash
pg_dump -U postgres water_counter_bot > backup.sql
```

### Восстановление из резервной копии:

```bash
psql -U postgres water_counter_bot < backup.sql
```

## Структура проекта после установки

```
tg-bot/
├── src/                  # Исходный код
├── dist/                 # Скомпилированный код (после npm run build)
├── uploads/              # Загруженные фото
├── node_modules/         # Зависимости
├── .env                  # Конфигурация (создать вручную)
├── package.json          # Зависимости проекта
└── tsconfig.json         # Конфигурация TypeScript
```

## Следующие шаги

1. Добавьте аппараты через меню "Управление аппаратами"
2. Создайте приглашения для сотрудников через меню "Управление пользователями"
3. Отправьте ссылки-приглашения вашим сотрудникам
4. Начните загружать показания!

---

**Поддержка:** Если у вас возникли проблемы, проверьте логи в консоли - там будут подробные сообщения об ошибках.
