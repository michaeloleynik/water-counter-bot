# 🚀 Быстрый старт

## 1. Создайте бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Укажите имя и username бота
4. **Сохраните токен!**

## 2. Узнайте свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Отправьте любое сообщение
3. **Сохраните ваш ID!**

## 3. Выберите способ установки

### 🐳 Вариант A: Docker Compose (Рекомендуется)

**Преимущества:** Всё в одной команде, не нужно устанавливать PostgreSQL отдельно.

```bash
# 1. Создайте .env файл
copy .env.example .env
# Отредактируйте: BOT_TOKEN, DEFAULT_ADMIN_TELEGRAM_ID

# 2. Запустите
docker-compose up -d

# 3. Проверьте логи
docker-compose logs -f app

# ✅ Готово! Бот работает на http://localhost:3000
```

📖 Подробнее: [DOCKER.md](DOCKER.md)

---

### 💻 Вариант B: Обычная установка

**Преимущества:** Полный контроль, удобно для разработки.

Скачайте и установите [PostgreSQL](https://www.postgresql.org/download/)

**Или используйте Docker только для БД:**
```bash
docker run --name water-counter-db \
  -e POSTGRES_PASSWORD=mypassword \
  -e POSTGRES_DB=water_counter_bot \
  -p 5432:5432 -d postgres:15
```

## 4. Настройте проект

```bash
# Установите зависимости
npm install

# Создайте .env файл
copy .env.example .env
```

Откройте `.env` и заполните:

```env
BOT_TOKEN=ваш_токен_от_BotFather
DEFAULT_ADMIN_TELEGRAM_ID=ваш_telegram_id
DB_PASSWORD=mypassword
```

## 5. Инициализируйте базу данных

```bash
npm run db:migrate
```

## 6. Запустите бота

```bash
npm run dev
```

## 7. Протестируйте

1. Найдите своего бота в Telegram
2. Отправьте `/start`
3. Если всё настроено правильно, вы увидите главное меню
4. Готово! 🎉

**Проверка API:**
```bash
curl http://localhost:3000/api/health
# Должно вернуть: {"status":"ok"}
```

---

## Что дальше?

1. **Добавьте аппараты:** `Управление аппаратами → Добавить аппарат`
2. **Пригласите сотрудников:** `Управление пользователями → Пригласить сотрудника`
3. **Загрузите показания:** `Загрузить показания`
4. **Просмотрите отчеты:** `Отчеты`

## Проблемы?

Смотрите подробную инструкцию в [INSTALL.md](INSTALL.md)

## Архитектура проекта

```
tg-bot/
├── src/                    # Backend (Telegram Bot)
│   ├── bot/               # Обработчики бота
│   ├── database/          # Подключение к БД
│   ├── models/            # Модели данных
│   ├── services/          # Бизнес-логика
│   ├── middleware/        # Проверка прав
│   └── utils/             # Утилиты
├── mini-app/              # Frontend (Telegram Mini App - в разработке)
├── uploads/               # Загруженные фото
└── .env                   # Конфигурация
```

## Основные команды

```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка проекта
npm start            # Запуск production
npm run db:migrate   # Создание таблиц БД
```

## Возможности

✅ **Для админов:**
- Управление аппаратами
- Управление пользователями
- Просмотр отчетов
- Статистика

✅ **Для сотрудников:**
- Загрузка показаний счетчика
- Прикрепление фото (обязательно)
- Просмотр своих показаний

✅ **Технические:**
- Система ролей
- Авторизация через приглашения
- База данных PostgreSQL
- Хранение фото на сервере

## Планы развития

🔜 **Скоро:**
- Telegram Mini App с offline-режимом
- Графики и аналитика
- Экспорт в Excel
- Уведомления и напоминания

Подробнее в [FEATURES.md](FEATURES.md)

---

**Лицензия:** MIT  
**Технологии:** Node.js, TypeScript, Telegraf, PostgreSQL
