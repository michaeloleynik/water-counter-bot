# Water Counter Mini App

Telegram Mini App для учета показаний счетчиков с поддержкой offline-режима.

## Возможности

- 📱 Современный интерфейс внутри Telegram
- 💾 Работа в offline-режиме (IndexedDB)
- 🔄 Автоматическая синхронизация при появлении интернета
- 📷 Улучшенная работа с фото
- 📊 Интерактивные графики

## Технологии

- **React 18** - UI фреймворк
- **TypeScript** - типизация
- **Vite** - сборщик
- **Dexie** - работа с IndexedDB (offline storage)
- **Telegram Web Apps API** - интеграция с Telegram

## Установка

```bash
cd mini-app
npm install
```

## Разработка

```bash
npm run dev
```

Mini App будет доступен на `http://localhost:5173`

## Сборка

```bash
npm run build
```

Результат сборки будет в папке `dist/`

## Структура

```
mini-app/
├── src/
│   ├── components/      # React компоненты
│   ├── services/        # Сервисы (API, DB)
│   ├── hooks/           # Custom hooks
│   ├── types/           # TypeScript типы
│   ├── utils/           # Утилиты
│   ├── db/              # IndexedDB схема
│   ├── App.tsx          # Главный компонент
│   └── main.tsx         # Точка входа
├── public/              # Статические файлы
└── index.html           # HTML шаблон
```

## Offline-режим

Mini App использует IndexedDB через Dexie для хранения данных локально:

1. Показания сохраняются локально при отсутствии интернета
2. Фотографии конвертируются в base64 и хранятся в IndexedDB
3. При появлении интернета данные автоматически синхронизируются
4. После успешной синхронизации локальные данные помечаются как синхронизированные

## Интеграция с ботом

Для запуска Mini App из бота используйте кнопку с типом `web_app`:

```typescript
Markup.button.webApp('📱 Открыть приложение', 'https://your-domain.com')
```

## Деплой

Mini App можно задеплоить на:
- GitHub Pages
- Vercel
- Netlify
- Cloudflare Pages

**Важно:** URL должен быть HTTPS!

## TODO

- [ ] Базовый UI
- [ ] Интеграция с Telegram Web Apps API
- [ ] Настройка IndexedDB (Dexie)
- [ ] Offline-синхронизация
- [ ] Сжатие и оптимизация фото
- [ ] Графики Chart.js
- [ ] Карта Leaflet
- [ ] PWA манифест
